import Editor from "@/components/user_ui/Editor";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="max-h-screen">
      <div className="max-h-screen">
        <Editor />
      </div>
    </div>
  );
};

export default page;
